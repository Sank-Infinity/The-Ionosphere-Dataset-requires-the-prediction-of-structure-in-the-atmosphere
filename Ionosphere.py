# -*- coding: utf-8 -*-
"""ANN_ISO

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1XjaW9k1YnjPAtUmPzv-4zpp2IYqhMPC3
"""

# Import required libraries
import numpy as np
import pandas as pd

# Importing dataset 
dataset = pd.read_csv('/content/drive/My Drive/Colab Notebooks/ionosphere.csv')

# Seperating feature of matrics and dependent variable 
X = dataset.iloc[: , :-1].values
Y = dataset.iloc[:, -1].values

T = []
for i in range(0,len(Y)):
  if Y[i]=='g':
    T.append(1)
  else:
    T.append(0)

T = np.asarray(T)

# Splitting dataset into training and test set
from sklearn.model_selection import train_test_split
X_train, X_test, Y_train, Y_test = train_test_split(X,T, test_size = 0.20, random_state= 0)

# Importing logistic regressor 
from sklearn.ensemble import RandomForestClassifier
classifier =RandomForestClassifier(n_estimators=300, criterion='entropy', random_state=0)
classifier.fit(X_train, Y_train)

# Predicting the result
Y_pred = classifier.predict(X_test)

Y_pred , Y_test

# cross validating results using confusion matrix
from sklearn.metrics import confusion_matrix
cm  = confusion_matrix(Y_test , Y_pred)

print(cm)

print('{} % accuracy.'.format(((cm[0][0]+cm[1][1])/len(Y_pred))*100))

